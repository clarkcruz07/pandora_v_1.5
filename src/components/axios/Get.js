import React,{useEffect} from 'react'

/*imports*/
/*components*/
const Get = (service, setMobileNumber, duration) =>{
    useEffect(() => {
        console.log(service + " = " + setMobileNumber + " = " + duration)
    })
    return (
        <div>
        </div>
    )
}

export default Get